summary: ZIP folder
security: []
tags:
  - Assets
operationId: zipFolder
description: ZIP folder
parameters:
  - name: id
    in: path
    description: The id of the folder.
    required: true
    schema:
      type: string
responses:
  "200":
    description: Successful request
    content:
      text/plain:
        schema:
          type: string
  "404":
    $ref: ../../../components/responses.yaml#/NotFoundError
x-codeSamples:
  - label: Directus SDK
    lang: JavaScript
    source: |
      import { createDirectus, rest, zipFolder } from '@directus/sdk';

      const client = createDirectus('directus_project_url').with(rest());

      const result = await client.request(zipFolder('<folder-id>'));
  - label: GraphQL
    lang: GraphQL
    source: Not available in Directus SDK/GraphQL
