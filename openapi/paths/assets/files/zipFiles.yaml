summary: ZIP files
security: []
tags:
  - Assets
operationId: zipFiles
description: ZIP files
requestBody:
  content:
    application/json:
      ids:
        type: array
        items:
          type: string
responses:
  "200":
    description: Successful request
    content:
      text/plain:
        schema:
          type: string
  "404":
    $ref: ../../../components/responses.yaml#/NotFoundError
x-codeSamples:
  - label: Directus SDK
    lang: JavaScript
    source: |
      import { createDirectus, rest, zipFiles } from '@directus/sdk';

      const client = createDirectus('directus_project_url').with(rest());

      const result = await client.request(zipFiles(['<file-id>','<file-id']));
  - label: GraphQL
    lang: GraphQL
    source: Not available in Directus SDK/GraphQL
